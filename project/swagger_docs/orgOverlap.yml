Endpoint for pulling all orgs with common data elements
---
parameters:
    - name: body
      in: body
      required: False
      schema: 
        type: object
        properties:
          authors:
            type: array
            items:
              $ref: '#/definitions/UniqueIdentifier'
          grants:
            type: array
            items:
              $ref: '#/definitions/UniqueIdentifier'
          terms:
            type: array
            items:
              $ref: '#/definitions/UniqueIdentifier'
          dates:
            $ref: '#/definitions/DateRange'
responses:
  200:
    description: A list of all orgs that match the filter
    schema:
      $ref: '#/definitions/DataList'
    # examples:
    #     application/json: {'papers': [{"id": "https://midasnetwork.us/papers/The-first-MIDAS-paper-ever",
    #                         "name": "The first MIDAS paper ever."},
    #                         {"id": "The-first-MIDAS-paper-ever---Response",
    #                         "name": "The first MIDAS paper ever - response."}]}